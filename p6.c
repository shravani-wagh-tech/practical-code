#include <stdio.h>
int main()
{
    int a,b;
    printf("enter two no:");
    scanf("%d%d",&a,&b);
    printf("addition = %d\n",a+b);
    printf("sub = %d\n",a-b);
    printf("mul = %d\n", a*b);
    return 0;
}